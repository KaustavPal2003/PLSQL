CREATE OR REPLACE FUNCTION PRIME(P IN NUMBER) RETURN BOOLEAN IS

FLAG BOOLEAN;
N NUMBER;

BEGIN

FLAG:=FALSE;
N:=2;

WHILE N<=FLOOR(P/2)
LOOP

IF(MOD(P,N)=0) THEN
FLAG:=TRUE;
EXIT;
END IF;

N:=N+1;
END LOOP;

RETURN FLAG;

END;
/



